[project]
name = "repo-onboarding-mcp"
version = "0.1.0"
description = "Read-only MCP server for local repository onboarding + OpenAI Agents SDK demo"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0",
    "openai-agents>=0.4",
    "python-dotenv>=1.0",
]
